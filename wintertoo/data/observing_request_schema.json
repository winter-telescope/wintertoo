{
  "type": "object",
  "properties": {
    "obsHistID": {"type": "integer"},
    "requestID": {"type": "integer"},
    "fieldRA": {"type": "number", "comment": "Radians"},
    "fieldDec": {"type": "number", "comment": "Radians"},
    "azimuth": {"type": "number", "comment":  "Degrees", "default":  "null"},
    "altitude": {"type": "number", "comment":  "Degrees"},
    "filter": {"type": "string", "comment": "'u'/'g'/'r'/'i'"},
    "visitExpTime": {"type": "number", "comment": "Total observation time (seconds)", "default": 30},
    "priority": {"type": "number", "comment": "Priority of observation"},
    "programPI": {"type": "string", "comment": "PI of observation"},
    "validStart": {"type": "number", "comment": "Format: MJD"},
    "validStop": {"type": "number", "comment": "Format: MJD"},
    "observed": {"type":  "boolean", "default":  false},
    "maxAirmass": {"type":  "number", "comment": "Maximum airmass for observation", "default":  2.0},
    "dither": {"type": "boolean", "comment": "Format: bool e.g. 'N'", "default":  true},
    "ditherStepSize": {"type": "number", "default": "null"},
    "fieldID": {"type": "integer", "default": -99999}
  },
  "required": [
    "obsHistID",
    "requestID",
    "fieldRA",
    "fieldDec",
    "azimuth",
    "altitude",
    "filter",
    "visitExpTime",
    "priority",
    "validStart",
    "validStop",
    "observed",
    "dither"
  ],
  "additionalProperties": false
}
